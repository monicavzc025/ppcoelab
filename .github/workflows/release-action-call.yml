name: Release action
# Call the reusable workflow release-solution-with-inputs.yml
# Release your solution to prod when you create a new release.

on:
  release:
      types: [created]        
permissions:
  contents: write
jobs:
  Release-solution-ALMLab:
    uses: ./.github/workflows/release-solution-to-prod-with-inputs.yml
    with:
      #You can specify the solution name here
      solution_name: TestSolutionOlman
      #Update your values here
      BUILD_ENVIRONMENT_URL: 'https://devusalm.crm.dynamics.com/'
      PRODUCTION_ENVIRONMENT_URL: 'https://produsalm.crm.dynamics.com/'
      CLIENT_ID: '13c5f4f3-7fd4-4c0d-977f-bb1782f411ce'
      TENANT_ID: '548d26ab-8caa-49e1-97c2-a1b1a06cc39c'     
    secrets:
      envSecret: ${{ secrets.PPCOES }}
